import { kessokuRamenMetadata } from '@/lib/metadata'

export const metadata = kessokuRamenMetadata

export default function KessokuRamenLayout({ children }: { children: React.ReactNode }) {
  return children
}
