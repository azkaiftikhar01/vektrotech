import { premierHealthMetadata } from '@/lib/metadata'

export const metadata = premierHealthMetadata

export default function PremierHealthLayout({ children }: { children: React.ReactNode }) {
  return children
}
